import AsyncStorage from '@react-native-async-storage/async-storage';
import { Redirect, router } from 'expo-router';
import { useEffect, useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';
import { Button, Loading } from '@/components/ui'; import { colors } from '@/theme';
export default function Onboarding(){const [seen,setSeen]=useState<boolean|null>(null);useEffect(()=>{AsyncStorage.getItem('onboarding:v1').then(v=>setSeen(v==='1'))},[]);if(seen===null)return <Loading/>;if(seen)return <Redirect href="/(tabs)"/>;const go=async(path:'/(tabs)'|'/auth')=>{await AsyncStorage.setItem('onboarding:v1','1');router.replace(path)};return <View style={s.page}><View style={s.logo}><Text style={s.logoText}>B</Text></View><Text style={s.brand}>BLACKSPOT YOU</Text><Text style={s.title}>Des lieux. Des vécus. Une communauté.</Text><Text style={s.body}>Découvrez des lieux documentés par la communauté, partagez votre expérience et contribuez à une base plus juste et plus utile.</Text><Button title="Découvrir les lieux" onPress={()=>go('/(tabs)')}/><Button title="Créer un compte ou se connecter" variant="secondary" onPress={()=>go('/auth')}/><Text style={s.note}>La géolocalisation reste facultative et n’est demandée que pour les lieux à proximité.</Text></View>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:colors.canvas,padding:28,justifyContent:'center',gap:16},logo:{width:72,height:72,borderRadius:24,backgroundColor:colors.purple,alignItems:'center',justifyContent:'center'},logoText:{fontSize:38,fontWeight:'900',color:colors.white},brand:{color:colors.purpleDark,fontWeight:'900',letterSpacing:2},title:{fontSize:38,fontWeight:'900',color:colors.ink,lineHeight:43,letterSpacing:-1},body:{fontSize:17,lineHeight:26,color:colors.muted,marginBottom:10},note:{textAlign:'center',color:colors.muted,fontSize:12,marginTop:8}});
