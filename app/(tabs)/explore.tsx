import { useEffect,useState } from 'react'; import { FlatList,StyleSheet,TextInput,View } from 'react-native'; import { PlaceCard } from '@/components/PlaceCard'; import { Empty,Loading,Title } from '@/components/ui'; import { listPlaces } from '@/services/places'; import { colors,radius } from '@/theme'; import type { Establishment } from '@/types';
export default function Explore(){const [query,setQuery]=useState('');const [places,setPlaces]=useState<Establishment[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{const id=setTimeout(()=>{setLoading(true);listPlaces({query}).then(setPlaces).finally(()=>setLoading(false))},300);return()=>clearTimeout(id)},[query]);return <View style={s.page}><Title>Explorer</Title><TextInput accessibilityLabel="Rechercher un lieu ou une ville" placeholder="Lieu, ville ou adresse…" placeholderTextColor={colors.muted} value={query} onChangeText={setQuery} style={s.search}/>{loading?<Loading/>:<FlatList data={places} keyExtractor={p=>p.id} renderItem={({item})=><PlaceCard place={item}/>} ListEmptyComponent={<Empty text="Aucun résultat. Essayez une autre recherche."/>} contentContainerStyle={s.list}/>}</View>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:colors.canvas,paddingTop:64,paddingHorizontal:20},search:{height:54,borderRadius:radius.pill,backgroundColor:colors.white,borderWidth:1,borderColor:colors.border,paddingHorizontal:18,fontSize:16,color:colors.ink,marginVertical:18},list:{paddingBottom:120}});
